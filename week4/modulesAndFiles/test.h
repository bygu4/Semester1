#pragma once
#pragma execution_character_set("utf-8")
#include <stdbool.h>

// тест для чтения из файла, для сортировки и бинарного поиска
// если есть ошибка, записывает вывод по переданному адресу
bool test(char* const output);
