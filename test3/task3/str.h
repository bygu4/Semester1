#pragma once

#include <stdbool.h>

// считать строку из стандартного потока
char* getString(const char breakPoint);

// проверка строк на равенство
bool stringsAreEqual(const char* const string1, const char* const string2);
