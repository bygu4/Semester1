#pragma once

#include <stdbool.h>
#include <stdlib.h>

// отсортировать массив по возрастанию. Возвращает наличие ошибки при выделении памяти
bool sort(int* const array, const size_t lengthOfArray);
